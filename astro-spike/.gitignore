node_modules/
dist/
.astro/

# Generated by `pagefind --site dist` and mirrored into public/ so the dev
# server can serve search. Build output, not source: `npm run build` recreates it.
public/pagefind/
