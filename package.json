{
  "name": "lucasbardella-com",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "components": "node build.js",
    "build": "node build.js && .dev/node-run observable build",
    "dev": ".dev/node-run observable preview",
    "deploy": ".dev/node-run observable deploy",
    "observable": ".dev/node-run observable"
  },
  "dependencies": {
    "@observablehq/framework": "^1.13.2",
    "d3-dsv": "^3.0.1",
    "d3-time-format": "^4.1.0",
    "glob": "^11.0.1"
  },
  "devDependencies": {
    "esbuild": "0.25.1"
  }
}
