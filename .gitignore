# node
node_modules

# astro
dist
.astro/
# search index, written into public/ by `npm run build` so the dev server can
# serve it; regenerated on every build
/public/pagefind/

# data
# Everything under public/data is written by `make data` and never committed.
# Static files that belong to the repository live in public/assets instead.
/public/data/
/src/data/

# cfd
# Simulation output, regenerated by `make cfd-cases` in the OpenFOAM image.
/cfd/cases

# rust
/target

# python
.python-version
__pycache__
*.pyc
*.pyo
*.egg-info

# latex, build artefacts of `make cv`
*.aux
*.dvi
*.fdb_latexmk
*.fls
*.gz
*.lof
*.toc
__latexindent_temp.tex

# misc
.DS_Store
*.log
.env
